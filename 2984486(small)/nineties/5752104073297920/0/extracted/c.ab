for c in 1..int(read_line()) {
    read_line()
    ary := read_line().split().map(int).to_array()
    count := 0
    for i in 0..ary.size-1 {
        if ary[i] > i
            count += 1
    }
    printf "Case #%d: %s\n", c, (count >= 510) ? "BAD" : "GOOD"
}
